// Minimal stub. Replace with real Anthropic client later.
export async function suggestTestsWithAnthropic(_prompt: string): Promise<{ files: Array<{ path: string; tests: string[] }> }> {
  return { files: [] };
}